---
// Footer.astro
---

<footer class="site-footer">
  <div class="footer-container">
    <div class="footer-grid">
      <div class="footer-section">
        <h3 class="footer-title">SEC<span class="accent">_</span>OPS</h3>
        <p class="footer-desc">Aprendiendo ciberseguridad autodidacta. Compartiendo conocimiento y proyectos.</p>
      </div>

      <div class="footer-section">
        <h4 class="section-title">Navegación</h4>
        <ul class="footer-links">
          <li><a href="/">Home</a></li>
          <li><a href="/proyectos">Proyectos</a></li>
          <li><a href="/blog">Blog</a></li>
          <li><a href="/contacto">Contacto</a></li>
        </ul>
      </div>

      <div class="footer-section">
        <h4 class="section-title">Social</h4>
        <ul class="footer-links">
          <li><a href="https://github.com" target="_blank">GitHub</a></li>
          <li><a href="https://linkedin.com" target="_blank">LinkedIn</a></li>
          <li><a href="https://twitter.com" target="_blank">Twitter</a></li>
        </ul>
      </div>

      <div class="footer-section">
        <h4 class="section-title">Contacto</h4>
        <p class="contact-info">contacto@secops.dev</p>
        <div class="status-indicator">
          <span class="status-dot"></span>
          <span>Disponible para proyectos</span>
        </div>
      </div>
    </div>

    <div class="footer-bottom">
      <p>&copy; 2024 SEC_OPS. Todos los derechos reservados.</p>
      <p class="terminal-text">root@secops:~$ whoami</p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--bg-secondary);
    border-top: 1px solid rgba(0, 255, 159, 0.1);
    margin-top: 8rem;
  }

  .footer-container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 4rem 2rem 2rem;
  }

  .footer-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 3rem;
    margin-bottom: 3rem;
  }

  .footer-section {
    animation: fadeInUp 0.8s ease backwards;
  }

  .footer-section:nth-child(2) { animation-delay: 0.1s; }
  .footer-section:nth-child(3) { animation-delay: 0.2s; }
  .footer-section:nth-child(4) { animation-delay: 0.3s; }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .footer-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: var(--text-primary);
  }

  .accent {
    color: var(--accent-primary);
  }

  .footer-desc {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .section-title {
    font-family: 'Space Mono', monospace;
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--accent-primary);
    margin-bottom: 1rem;
  }

  .footer-links {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .footer-links li {
    margin-bottom: 0.75rem;
  }

  .footer-links a {
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.3s ease;
    display: inline-block;
  }

  .footer-links a:hover {
    color: var(--accent-primary);
    transform: translateX(4px);
  }

  .contact-info {
    color: var(--text-secondary);
    margin-bottom: 1rem;
    font-family: 'Space Mono', monospace;
  }

  .status-indicator {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.9rem;
    color: var(--text-secondary);
  }

  .status-dot {
    width: 8px;
    height: 8px;
    background: var(--accent-primary);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(1.2); }
  }

  .footer-bottom {
    padding-top: 2rem;
    border-top: 1px solid rgba(0, 255, 159, 0.1);
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: var(--text-secondary);
    font-size: 0.9rem;
  }

  .terminal-text {
    font-family: 'JetBrains Mono', monospace;
    color: var(--accent-primary);
  }

  @media (max-width: 768px) {
    .footer-container {
      padding: 3rem 1.5rem 1.5rem;
    }

    .footer-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .footer-bottom {
      flex-direction: column;
      gap: 1rem;
      text-align: center;
    }
  }
</style>