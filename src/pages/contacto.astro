---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contacto - SEC_OPS">
  <section class="contacto-section">
    <div class="container">
      <div class="contacto-grid">
        <div class="info-column">
          <div class="header-content">
            <h1 class="page-title">
              <span class="terminal-prompt">$</span> ./connect.sh
            </h1>
            <p class="page-description">
              ¿Tienes un proyecto en mente? ¿Quieres colaborar o simplemente charlar sobre ciberseguridad? 
              ¡Conectemos!
            </p>
          </div>

          <div class="info-cards">
            <div class="info-card">
              <div class="card-icon">📧</div>
              <h3 class="card-title">Email</h3>
              <a href="mailto:contacto@secops.dev" class="card-link">contacto@secops.dev</a>
            </div>

            <div class="info-card">
              <div class="card-icon">💼</div>
              <h3 class="card-title">LinkedIn</h3>
              <a href="https://linkedin.com/in/tu-perfil" target="_blank" class="card-link">linkedin.com/in/tu-perfil</a>
            </div>

            <div class="info-card">
              <div class="card-icon">🐙</div>
              <h3 class="card-title">GitHub</h3>
              <a href="https://github.com/delsazdev" target="_blank" class="card-link">github.com/delsazdev</a>
            </div>

            <div class="info-card">
              <div class="card-icon">🐦</div>
              <h3 class="card-title">Twitter</h3>
              <a href="https://twitter.com/tu_usuario" target="_blank" class="card-link">@tu_usuario</a>
            </div>
          </div>

          <div class="disponibilidad">
            <div class="status-header">
              <span class="status-dot"></span>
              <span class="status-text">Estado actual</span>
            </div>
            <p class="status-message">
              Disponible para proyectos freelance y colaboraciones en ciberseguridad
            </p>
          </div>

          <div class="horario">
            <h3 class="horario-title">⏰ Horario de respuesta</h3>
            <p class="horario-text">
              Respondo mensajes de lunes a viernes, generalmente en 24-48 horas.
            </p>
          </div>
        </div>

        <!-- === FORMULARIO MEJORADO === -->
        <div class="form-column">
          <div class="form-container">
            <div class="form-header">
              <span class="terminal-symbol">&gt;</span>
              <span class="form-title">Enviar mensaje</span>
            </div>

            <form id="contact-form" class="contact-form">
              <!-- Honeypot anti-spam -->
              <input type="text" name="_gotcha" style="display: none;" />

              <!-- Nombre -->
              <div class="form-group">
                <label for="nombre" class="form-label">Nombre *</label>
                <input 
                  type="text" 
                  id="nombre" 
                  name="nombre" 
                  class="form-input" 
                  placeholder="Tu nombre completo"
                  required
                />
              </div>

              <!-- Email -->
              <div class="form-group">
                <label for="email" class="form-label">Email *</label>
                <input 
                  type="email" 
                  id="email" 
                  name="email" 
                  class="form-input" 
                  placeholder="tu@email.com"
                  required
                />
              </div>

              <!-- Asunto -->
              <div class="form-group">
                <label for="asunto" class="form-label">Asunto *</label>
                <select id="asunto" name="asunto" class="form-select" required>
                  <option value="">Selecciona un asunto</option>
                  <option value="colaboracion">Colaboración</option>
                  <option value="proyecto">Proyecto freelance</option>
                  <option value="consulta">Consulta técnica</option>
                  <option value="networking">Networking</option>
                  <option value="otro">Otro</option>
                </select>
              </div>

              <!-- Mensaje -->
              <div class="form-group">
                <label for="mensaje" class="form-label">Mensaje *</label>
                <textarea 
                  id="mensaje" 
                  name="mensaje" 
                  class="form-textarea" 
                  rows="7"
                  placeholder="Cuéntame en qué puedo ayudarte..."
                  required
                ></textarea>
              </div>

              <!-- Botón mejorado -->
              <button type="submit" class="submit-btn">
                <span class="btn-text">Enviar mensaje</span>
                <svg class="btn-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <line x1="22" y1="2" x2="11" y2="13"></line>
                  <polygon points="22 2 15 22 11 13 2 9 22 2"></polygon>
                </svg>
              </button>

              <div id="form-message" class="form-message"></div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* === Tus estilos originales (sin cambios) + mejoras al formulario === */

  .contacto-section {
    padding: 4rem 0;
    min-height: calc(100vh - 80px);
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .contacto-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .info-column {
    animation: fadeInLeft 0.8s ease;
  }

  @keyframes fadeInLeft {
    from {
      opacity: 0;
      transform: translateX(-30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  /* ... (tus otros estilos del header, cards, disponibilidad, etc. se mantienen iguales) ... */

  /* === Mejoras específicas al formulario === */

  .form-container {
    background: var(--bg-secondary);
    border: 1px solid rgba(0, 255, 159, 0.18);
    border-radius: 12px;
    padding: 2.2rem;
    box-shadow: 0 4px 20px rgba(0, 255, 159, 0.08);
    transition: all 0.3s ease;
  }

  .form-container:hover {
    border-color: rgba(0, 255, 159, 0.45);
    box-shadow: 0 10px 35px rgba(0, 255, 159, 0.15);
  }

  .form-input,
  .form-textarea,
  .form-select {
    width: 100%;
    padding: 1rem 1.2rem;
    background: rgba(10, 25, 20, 0.65);
    border: 1px solid rgba(0, 255, 159, 0.25);
    border-radius: 8px;
    color: var(--text-primary);
    font-family: 'JetBrains Mono', monospace;
    font-size: 1rem;
    transition: all 0.3s ease;
  }

  .form-input:focus,
  .form-textarea:focus,
  .form-select:focus {
    outline: none;
    border-color: var(--accent-primary);
    box-shadow: 0 0 0 4px rgba(0, 255, 159, 0.18);
    background: rgba(10, 25, 20, 0.85);
  }

  .form-label {
    display: block;
    margin-bottom: 0.6rem;
    color: var(--text-secondary);
    font-size: 0.9rem;
    font-family: 'JetBrains Mono', monospace;
    text-transform: uppercase;
    letter-spacing: 0.8px;
  }

  .form-textarea {
    resize: vertical;
    min-height: 150px;
  }

  .submit-btn {
    width: 100%;
    padding: 1.2rem;
    background: linear-gradient(135deg, var(--accent-primary), #00c853);
    border: none;
    border-radius: 8px;
    color: #000;
    font-family: 'JetBrains Mono', monospace;
    font-weight: bold;
    font-size: 1.1rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.8rem;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .submit-btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(0, 255, 159, 0.4);
    background: linear-gradient(135deg, #00ff9f, #00c853);
  }

  .form-message {
    margin-top: 1.2rem;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.95rem;
    text-align: center;
  }

  .form-message.success {
    color: var(--accent-primary);
  }

  .form-message.error {
    color: #ff5555;
  }
</style>