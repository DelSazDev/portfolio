---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contacto - SEC_OPS">
  <section class="contacto-section">
    <div class="container">
      <div class="contacto-grid">
        <div class="info-column">
          <div class="header-content">
            <h1 class="page-title">
              <span class="terminal-prompt">$</span> ./connect.sh
            </h1>
            <p class="page-description">
              ¿Tienes un proyecto en mente? ¿Quieres colaborar o simplemente charlar sobre ciberseguridad? 
              ¡Conectemos!
            </p>
          </div>

          <div class="info-cards">
            <div class="info-card">
              <div class="card-icon">📧</div>
              <h3 class="card-title">Email</h3>
              <a href="mailto:contacto@secops.dev" class="card-link">contacto@secops.dev</a>
            </div>

            <div class="info-card">
              <div class="card-icon">💼</div>
              <h3 class="card-title">LinkedIn</h3>
              <a href="https://linkedin.com" target="_blank" class="card-link">linkedin.com/in/tu-perfil</a>
            </div>

            <div class="info-card">
              <div class="card-icon">🐙</div>
              <h3 class="card-title">GitHub</h3>
              <a href="https://github.com" target="_blank" class="card-link">github.com/tu-usuario</a>
            </div>

            <div class="info-card">
              <div class="card-icon">🐦</div>
              <h3 class="card-title">Twitter</h3>
              <a href="https://twitter.com" target="_blank" class="card-link">@tu_usuario</a>
            </div>
          </div>

          <div class="disponibilidad">
            <div class="status-header">
              <span class="status-dot"></span>
              <span class="status-text">Estado actual</span>
            </div>
            <p class="status-message">
              Disponible para proyectos freelance y colaboraciones en ciberseguridad
            </p>
          </div>

          <div class="horario">
            <h3 class="horario-title">⏰ Horario de respuesta</h3>
            <p class="horario-text">
              Respondo mensajes de lunes a viernes, generalmente en 24-48 horas.
            </p>
          </div>
        </div>

        <div class="form-column">
          <div class="form-container">
            <div class="form-header">
              <span class="terminal-symbol">&gt;</span>
              <span class="form-title">Enviar mensaje</span>
            </div>

            <form id="contactForm" class="contact-form" action="https://formspree.io/f/xanyanjr" method="POST">
              <div class="form-group">
                <label for="nombre" class="form-label">Nombre *</label>
                <input 
                  type="text" 
                  id="nombre" 
                  name="nombre" 
                  class="form-input" 
                  placeholder="Tu nombre completo"
                  required
                />
              </div>

              <div class="form-group">
                <label for="email" class="form-label">Email *</label>
                <input 
                  type="email" 
                  id="email" 
                  name="email" 
                  class="form-input" 
                  placeholder="tu@email.com"
                  required
                />
              </div>

              <div class="form-group">
                <label for="asunto" class="form-label">Asunto *</label>
                <select id="asunto" name="asunto" class="form-select" required>
                  <option value="">Selecciona un asunto</option>
                  <option value="colaboracion">Colaboración</option>
                  <option value="proyecto">Proyecto freelance</option>
                  <option value="consulta">Consulta técnica</option>
                  <option value="networking">Networking</option>
                  <option value="otro">Otro</option>
                </select>
              </div>

              <div class="form-group">
                <label for="mensaje" class="form-label">Mensaje *</label>
                <textarea 
                  id="mensaje" 
                  name="mensaje" 
                  class="form-textarea" 
                  rows="6"
                  placeholder="Cuéntame en qué puedo ayudarte..."
                  required
                ></textarea>
              </div>

              <button type="submit" class="submit-btn">
                <span class="btn-text">Enviar mensaje</span>
                <svg class="btn-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <line x1="22" y1="2" x2="11" y2="13"></line>
                  <polygon points="22 2 15 22 11 13 2 9 22 2"></polygon>
                </svg>
              </button>

              <div id="formStatus" class="form-message"></div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .contacto-section {
    padding: 4rem 0;
    min-height: calc(100vh - 80px);
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .contacto-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: start;
  }

  .info-column {
    animation: fadeInLeft 0.8s ease;
  }

  @keyframes fadeInLeft {
    from {
      opacity: 0;
      transform: translateX(-30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  .header-content {
    margin-bottom: 3rem;
  }

  .page-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: clamp(2rem, 5vw, 3rem);
    color: var(--text-primary);
    margin-bottom: 1rem;
  }

  .terminal-prompt {
    color: var(--accent-primary);
    margin-right: 0.5rem;
  }

  .page-description {
    font-size: 1.1rem;
    color: var(--text-secondary);
    line-height: 1.7;
  }

  .info-cards {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .info-card {
    background: var(--bg-secondary);
    border: 1px solid rgba(0, 255, 159, 0.2);
    border-radius: 8px;
    padding: 1.5rem;
    transition: all 0.3s ease;
  }

  .info-card:hover {
    border-color: var(--accent-primary);
    transform: translateY(-3px);
    box-shadow: 0 5px 20px rgba(0, 255, 159, 0.15);
  }

  .card-icon {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }

  .card-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.9rem;
    color: var(--text-secondary);
    margin-bottom: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .card-link {
    color: var(--accent-primary);
    text-decoration: none;
    font-size: 0.95rem;
    transition: color 0.3s ease;
    word-break: break-all;
  }

  .card-link:hover {
    color: var(--accent-secondary);
  }

  .disponibilidad {
    background: var(--bg-secondary);
    border: 1px solid rgba(0, 255, 159, 0.2);
    border-radius: 8px;
    padding: 1.5rem;
    margin-bottom: 1.5rem;
  }

  .status-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
  }

  .status-dot {
    width: 10px;
    height: 10px;
    background: var(--accent-primary);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(1.3); }
  }

  .status-text {
    font-family: 'JetBrains Mono', monospace;
    color: var(--text-primary);
    font-weight: 700;
    font-size: 0.95rem;
  }

  .status-message {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .horario {
    background: rgba(0, 217, 255, 0.05);
    border: 1px solid rgba(0, 217, 255, 0.2);
    border-radius: 8px;
    padding: 1.5rem;
  }

  .horario-title {
    font-family: 'JetBrains Mono', monospace;
    color: var(--accent-secondary);
    font-size: 1rem;
    margin-bottom: 0.75rem;
  }

  .horario-text {
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .form-column {
    animation: fadeInRight 0.8s ease;
  }

  @keyframes fadeInRight {
    from {
      opacity: 0;
      transform: translateX(30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  .form-container {
    background: var(--bg-secondary);
    border: 1px solid rgba(0, 255, 159, 0.2);
    border-radius: 8px;
    overflow: hidden;
    position: sticky;
    top: 100px;
  }

  .form-header {
    background: var(--bg-tertiary);
    padding: 1.25rem 1.5rem;
    border-bottom: 1px solid rgba(0, 255, 159, 0.2);
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .terminal-symbol {
    color: var(--accent-primary);
    font-family: 'JetBrains Mono', monospace;
    font-size: 1.5rem;
    font-weight: 700;
  }

  .form-title {
    font-family: 'JetBrains Mono', monospace;
    color: var(--text-primary);
    font-size: 1.1rem;
    font-weight: 700;
  }

  .contact-form {
    padding: 2rem 1.5rem;
  }

  .form-group {
    margin-bottom: 1.5rem;
  }

  .form-label {
    display: block;
    font-family: 'Space Mono', monospace;
    color: var(--text-primary);
    font-size: 0.9rem;
    margin-bottom: 0.5rem;
    font-weight: 700;
  }

  .form-input,
  .form-select,
  .form-textarea {
    width: 100%;
    padding: 0.875rem 1rem;
    background: var(--bg-primary);
    border: 1px solid rgba(0, 255, 159, 0.2);
    border-radius: 4px;
    color: var(--text-primary);
    font-family: 'Space Mono', monospace;
    font-size: 0.95rem;
    transition: all 0.3s ease;
  }

  .form-input:focus,
  .form-select:focus,
  .form-textarea:focus {
    outline: none;
    border-color: var(--accent-primary);
    box-shadow: 0 0 0 3px rgba(0, 255, 159, 0.1);
  }

  .form-input::placeholder,
  .form-textarea::placeholder {
    color: var(--text-secondary);
    opacity: 0.6;
  }

  .form-textarea {
    resize: vertical;
    min-height: 120px;
  }

  .submit-btn {
    width: 100%;
    padding: 1rem 2rem;
    background: var(--accent-primary);
    color: var(--bg-primary);
    border: 2px solid var(--accent-primary);
    border-radius: 4px;
    font-family: 'Space Mono', monospace;
    font-size: 1rem;
    font-weight: 700;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .submit-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s ease;
  }

  .submit-btn:hover::before {
    left: 100%;
  }

  .submit-btn:hover {
    background: transparent;
    color: var(--accent-primary);
    transform: translateY(-2px);
    box-shadow: 0 10px 30px rgba(0, 255, 159, 0.3);
  }

  .submit-btn:active {
    transform: translateY(0);
  }

  .form-message {
    margin-top: 1rem;
    padding: 1rem;
    border-radius: 4px;
    font-size: 0.9rem;
    text-align: center;
    display: none;
  }

  .form-message.success {
    display: block;
    background: rgba(0, 255, 159, 0.1);
    border: 1px solid var(--accent-primary);
    color: var(--accent-primary);
  }

  .form-message.error {
    display: block;
    background: rgba(255, 0, 110, 0.1);
    border: 1px solid var(--accent-danger);
    color: var(--accent-danger);
  }

  @media (max-width: 1024px) {
    .contacto-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .form-container {
      position: static;
    }

    .info-cards {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .container {
      padding: 0 1.5rem;
    }

    .page-title {
      font-size: 2rem;
    }

    .info-cards {
      gap: 1rem;
    }

    .contact-form {
      padding: 1.5rem 1rem;
    }
  }
</style>